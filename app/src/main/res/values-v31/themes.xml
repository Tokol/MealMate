<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MealMate.Splash" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowSplashScreenBackground">@color/primary</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/logo</item> <!-- Ensure lowercase filename -->
        <!-- Fixed attribute -->
    </style>
</resources>